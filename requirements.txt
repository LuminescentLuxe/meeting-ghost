# ─────────────────────────────────────────────
# Meeting Ghost — requirements.txt
# Install everything: pip install -r requirements.txt
# ─────────────────────────────────────────────

# ── Sprint 1: Transcription ──────────────────
faster-whisper>=1.0.0       # fast CPU/GPU Whisper inference
torch>=2.0.0                # needed by faster-whisper
gTTS>=2.5.0                 # generate test audio (optional)
pydub>=0.25.1               # audio manipulation

# ── Sprint 2: Speaker diarization ───────────
pyannote.audio>=3.1.0       # speaker diarization
# NOTE: requires a free HuggingFace token
# 1. Go to https://hf.co/pyannote/speaker-diarization-3.1
# 2. Accept the model license
# 3. Set env var: export HF_TOKEN=hf_your_token_here

# ── Sprint 3: Classification ─────────────────
transformers>=4.40.0        # zero-shot + fine-tuning
scikit-learn>=1.3.0         # metrics, train/test split
datasets>=2.19.0            # for fine-tuning in Sprint 5

# ── Sprint 4: Catch-up generator ────────────
anthropic>=0.25.0           # Claude API for summaries

# ── Sprint 5: Fine-tuning ────────────────────
accelerate>=0.27.0          # HuggingFace trainer backend

# ── Utilities ────────────────────────────────
python-dotenv>=1.0.0        # load .env file for API keys
pandas>=2.0.0               # data handling for labeled CSV
tqdm>=4.66.0                # progress bars
